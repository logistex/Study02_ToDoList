<!-- 2026-08-09 16:53 KST -->

# 구현 프롬프트 (5단계)

[PRD.md](PRD.md)를 클로드 코드에서 실행할 5개의 프롬프트로 나눈 것이다.

## 사용법

이 문서는 한 줄씩 붙여 넣는 대본이 아니라, 클로드에게 통째로 넘길 **구현 계획서**다.

- "이 계획에 따라 구현을 시작해 줘"라고 한 번 지시하면, 클로드가 1단계부터 5단계까지 순서대로 진행한다
- 각 단계의 **확인**과 **커밋**은 클로드가 함께 수행한다. 터미널을 열 필요는 없다
- 단계마다 올라오는 보고를 읽고, 어긋난 것이 있으면 그 자리에서 고치라고 지시한다. 뒤로 갈수록 원인을 찾기 어려워진다
- 구현이 끝나면 **브라우저에서 직접 확인한다**. 문서 끝의 「마무리 점검」이 그 자리다

이 앱은 빌드 도구와 의존성이 없는 단일 HTML 파일이라 테스트 러너를 두지 않는다. 단계별 검증은 클로드가 하고, 마지막 확인은 사람이 브라우저에서 한다.

## 공통 규약

아래는 5단계 전체에 적용되는 제약이다. 각 프롬프트에도 필요한 만큼 반복해 넣었지만, 클로드가 중간에 어긋나면 이 절을 다시 붙여 넣으면 된다.

**구조 제약**

- 파일은 `index.html` 하나뿐이다. CSS는 `<style>`, 자바스크립트는 `<script>`로 같은 파일 안에 둔다
- 외부 라이브러리와 프레임워크를 쓰지 않는다
- ES 모듈(`import` / `export`)을 쓰지 않는다. `file://`로 열면 CORS 정책에 막힌다
- UI 문구는 한국어로 쓴다

**이름 규약** (단계마다 이름이 어긋나지 않도록 고정한다)

| 종류 | 이름 |
|---|---|
| 상수 | `STORAGE_KEY` (`"todoApp.v1"`), `CATEGORY_LABELS` |
| 상태 | `todos`, `currentFilter`, `editingId` |
| 저장 | `loadTodos()`, `saveTodos()`, `createId()` |
| 화면 | `render()`, `renderList()`, `renderProgress()`, `getVisibleTodos()` |
| 동작 | `addTodo()`, `toggleTodo(id)`, `deleteTodo(id)`, `saveEdit(id)` |

**상태 변경 규칙**

데이터를 바꾸는 모든 동작은 예외 없이 같은 순서를 따른다.

```
배열 변경 → saveTodos() → render()
```

DOM을 부분적으로 고치지 않는다. 항목이 20개 안팎이라 전체를 다시 그려도 느려지지 않고, 화면과 데이터가 어긋날 여지가 없어진다.

---

## 1단계: 뼈대와 저장 계층

**목표** — 화면 틀을 잡고, 데이터를 localStorage에 저장하고 불러오는 부분을 먼저 완성한다. 아직 버튼은 동작하지 않는다.

````
index.html 파일 하나를 만들어서 할 일 관리 앱의 뼈대와 데이터 저장 계층을 구현해 줘.
같은 폴더의 PRD.md에 전체 요구사항이 있으니 참고해.

제약:
- 파일은 index.html 하나뿐이다. CSS는 <style>, 자바스크립트는 <script>로 같은 파일 안에 둔다
- 외부 라이브러리를 쓰지 않는다. ES 모듈(import/export)도 쓰지 않는다
- UI 문구는 한국어로 쓴다

HTML 구조 (내용은 비어 있어도 되고, 자리만 잡아 줘):
- 헤더: 앱 제목 "할 일 관리"
- 진행률 영역 (빈 컨테이너만. 4단계에서 채운다)
- 입력줄 영역 (빈 컨테이너만. 2단계에서 채운다)
- 필터 탭 영역 (빈 컨테이너만. 4단계에서 채운다)
- 목록: <ul id="todo-list">

CSS:
- 본문 최대 폭 600px, 가운데 정렬
- 읽기 편한 기본 여백과 글꼴만. 화려하게 만들지 않는다

자바스크립트로 아래를 정확히 이 이름으로 구현해 줘:
- const STORAGE_KEY = "todoApp.v1"
- const CATEGORY_LABELS = { work: "업무", personal: "개인", study: "공부" }
- let todos = []           // 할 일 배열
- let currentFilter = "all"  // 4단계에서 쓴다
- let editingId = null       // 5단계에서 쓴다
- createId()
    Date.now().toString(36) + Math.random().toString(36).slice(2, 6) 을 반환한다.
    crypto.randomUUID()는 file://에서 브라우저마다 동작이 갈릴 수 있으니 쓰지 마.
- loadTodos()
    localStorage에서 STORAGE_KEY를 읽어 todos에 넣는다.
    저장된 값이 없거나 JSON 파싱에 실패하면 빈 배열로 시작한다. 예외를 밖으로 던지지 않는다.
    저장 형식은 { version: 1, todos: [...] } 이다.
- saveTodos()
    { version: 1, todos } 를 JSON으로 STORAGE_KEY에 저장한다.
- renderList()
    todos를 <ul id="todo-list">에 다시 그린다. 이번 단계에서는 읽기 전용이다.
    각 항목은 <li>로 만들고 안에 할 일 텍스트와 카테고리 배지를 넣는다.
    배지 텍스트는 CATEGORY_LABELS로 변환한다. 카테고리 3종을 서로 다른 색으로 구분해.
    체크박스와 버튼은 아직 넣지 않는다.
- render()
    지금은 renderList()만 호출한다. 다음 단계에서 호출 대상이 늘어난다.

마지막에 loadTodos()와 render()를 한 번 호출해서 페이지를 열면 저장된 데이터가 뜨게 해 줘.

할 일 항목 하나의 형태:
{ id: 문자열, text: 문자열, category: "work"|"personal"|"study", done: 불리언, createdAt: 숫자 }
````

**확인** (클로드가 수행하고 결과를 보고한다) — `index.html`을 브라우저로 열고 개발자 도구 콘솔에서 실행한다.

```js
todos = [
  { id: createId(), text: "테스트 항목", category: "work", done: false, createdAt: Date.now() },
  { id: createId(), text: "두 번째",     category: "study", done: true,  createdAt: Date.now() }
];
saveTodos(); render();
```

1. 두 항목이 화면에 뜨고, 배지가 각각 업무와 공부로 나온다
2. **새로고침해도 두 항목이 그대로 있다** — 이 단계의 핵심이다
3. 콘솔에서 `localStorage.setItem("todoApp.v1", "깨진값")`을 실행하고 새로고침한다. 오류 없이 빈 목록으로 뜬다

**커밋**

```bash
git add index.html && git commit -m "feat: 앱 뼈대와 localStorage 저장 계층 구현"
```

---

## 2단계: 할 일 추가

**목표** — 입력줄을 만들어 실제로 할 일을 쌓을 수 있게 한다. 이 단계가 끝나면 콘솔 없이 앱을 쓸 수 있다.

````
index.html에 할 일 추가 기능을 구현해 줘. PRD.md의 F-1 항목이다.
기존 이름 규약(todos, saveTodos, render, renderList, CATEGORY_LABELS)을 그대로 쓰고,
새 파일을 만들지 마.

입력줄 UI를 헤더 아래 입력줄 영역에 넣어 줘:
- 텍스트 입력창: <input id="todo-input">, placeholder는 "할 일을 입력하세요"
- 카테고리 선택: <select id="category-select">, 업무/개인/공부 세 개.
  value는 각각 work / personal / study, 기본 선택은 업무
- 추가 버튼: <button id="add-btn">추가</button>

addTodo() 함수를 만들어서 아래대로 동작하게 해 줘:
- 입력창 텍스트의 앞뒤 공백을 잘라 낸다
- 잘라 낸 결과가 빈 문자열이면 아무 일도 하지 않고 함수를 끝낸다.
  경고창을 띄우지 마. 입력창 포커스만 유지한다
- 새 항목을 { id: createId(), text, category, done: false, createdAt: Date.now() } 로 만든다
- todos 배열의 맨 앞에 넣는다 (unshift). 최근에 적은 것이 위에 오게 하려는 것이다
- saveTodos() 후 render()
- 입력창을 비우고 포커스를 다시 입력창에 준다. 카테고리 선택은 그대로 둔다

호출 경로 두 가지:
- 추가 버튼 클릭
- 텍스트 입력창에서 Enter 키

renderList()에 빈 상태 처리를 추가해 줘:
- todos가 비어 있으면 목록 자리에 "할 일을 추가해 보세요" 안내 문구를 표시한다
- 문구는 흐린 색으로 가운데 정렬한다
````

**확인** (클로드가 수행하고 결과를 보고한다)

1. 할 일 3개를 서로 다른 카테고리로 추가한다. 배지 색이 카테고리마다 다르다
2. 마지막에 추가한 것이 맨 위에 있다
3. 추가한 뒤 바로 다음 할 일을 타이핑할 수 있다 (포커스가 입력창에 남아 있다)
4. Enter 키로도 추가된다
5. 빈 칸에서 추가를 누른다. 아무 일도 일어나지 않는다. 스페이스만 넣고 눌러도 마찬가지다
6. 새로고침해도 3개가 그대로다
7. 콘솔에서 `todos = []; saveTodos(); render();` 를 실행하면 안내 문구가 나온다

**커밋**

```bash
git add index.html && git commit -m "feat: 할 일 추가 기능과 빈 상태 표시"
```

---

## 3단계: 완료 체크와 삭제

**목표** — 목록 안의 조작을 붙인다. 이벤트를 항목마다 붙이지 않고 한 곳에서 처리하는 구조를 여기서 잡는다.

````
index.html에 완료 체크와 삭제 기능을 구현해 줘. PRD.md의 F-2와 F-4 항목이다.

renderList()가 그리는 각 <li>에 아래를 추가해 줘:
- 왼쪽에 체크박스. 항목의 done 값이 체크 상태에 반영되어야 한다
- 오른쪽에 삭제 버튼

이벤트 처리 방식이 중요해. 항목마다 addEventListener를 붙이지 마.
<ul id="todo-list">에 click 리스너를 하나만 걸고 이벤트 위임으로 처리해 줘.
- 각 <li>에 data-id 속성으로 항목 id를 넣는다
- 체크박스와 삭제 버튼에 data-action 속성을 넣는다 ("toggle", "delete")
- 리스너에서 event.target의 data-action을 보고 분기한다

함수 두 개:
- toggleTodo(id)
    해당 항목의 done을 뒤집는다. saveTodos() 후 render()
- deleteTodo(id)
    confirm()으로 "이 할 일을 삭제할까요?" 를 묻는다.
    사용자가 확인한 경우에만 배열에서 제거하고 saveTodos() 후 render().
    취소하면 아무 일도 하지 않는다.
    되돌리기는 만들지 마. 확인 창이 그 역할을 대신한다

완료된 항목의 표시:
- 텍스트에 취소선을 긋는다
- 글자를 흐리게 한다
- 목록에서 사라지게 하거나 아래로 내리지 마. 자리를 그대로 지킨다
````

**확인** (클로드가 수행하고 결과를 보고한다)

1. 체크박스를 누르면 취소선이 생기고, 다시 누르면 없어진다
2. 체크해도 항목의 위치가 바뀌지 않는다
3. 체크한 뒤 새로고침한다. 체크 상태가 유지된다
4. 삭제 버튼을 누르면 확인 창이 뜬다. 취소하면 항목이 남는다
5. 다시 눌러 확인하면 사라진다. 새로고침해도 사라진 상태다
6. 개발자 도구 Elements 탭에서 `<ul id="todo-list">`를 확인한다. 리스너가 `<ul>` 하나에만 붙어 있다

**커밋**

```bash
git add index.html && git commit -m "feat: 완료 체크와 삭제 기능 (이벤트 위임)"
```

---

## 4단계: 카테고리 필터와 진행률

**목표** — 카테고리별로 걸러 보고, 지금 얼마나 했는지 보여 준다. **이 앱에서 가장 틀리기 쉬운 단계다.**

````
index.html에 카테고리 필터와 진행률을 구현해 줘. PRD.md의 F-5와 F-6 항목이다.

필터 탭 UI를 입력줄 아래 필터 영역에 넣어 줘:
- 버튼 4개: 전체 / 업무 / 개인 / 공부
- 각 버튼에 data-filter 속성 (all / work / personal / study)
- 현재 선택된 탭은 색이나 밑줄로 구분되게 표시한다
- 클릭하면 currentFilter를 바꾸고 render()를 호출한다
- 필터 상태는 저장하지 않는다. 앱을 다시 열면 항상 전체다

getVisibleTodos() 함수:
- currentFilter가 "all"이면 todos 전체를 반환한다
- 아니면 category가 currentFilter와 같은 항목만 반환한다

renderList()를 고쳐 줘:
- todos 대신 getVisibleTodos() 결과를 그린다
- 빈 상태 문구를 두 가지로 나눈다.
  currentFilter가 "all"이면 "할 일을 추가해 보세요",
  특정 카테고리면 "이 카테고리에는 할 일이 없습니다"

renderProgress() 함수를 새로 만들어서 헤더 아래 진행률 영역에 그려 줘:
- 진행률 막대와 "완료 수 / 전체 수 (퍼센트)" 형식의 글자
- **계산 기준은 todos 전체가 아니라 getVisibleTodos() 결과다.**
  업무 탭을 보고 있으면 업무 항목만으로 계산해야 한다.
  화면에 보이는 목록과 숫자가 어긋나면 안 된다
- 퍼센트는 Math.round로 소수점 없이 반올림한다
- 해당 필터에 항목이 하나도 없으면 "0 / 0 (0%)"로 표시하고 막대는 빈 상태로 둔다.
  0으로 나누지 않도록 주의해
- 막대는 CSS width 퍼센트로 표현한다

render()가 renderProgress()와 renderList()를 둘 다 호출하게 고쳐 줘.
````

**확인** (클로드가 수행하고 결과를 보고한다) — 아래 데이터를 정확히 만들어 놓고 확인한다. 업무 3개 중 1개 완료, 개인 2개 중 2개 완료다.

| 탭 | 나와야 하는 값 |
|---|---|
| 전체 | `3 / 5 (60%)` |
| 업무 | `1 / 3 (33%)` |
| 개인 | `2 / 2 (100%)`, 막대가 꽉 참 |
| 공부 | `0 / 0 (0%)`, 막대는 빈 상태, "이 카테고리에는 할 일이 없습니다" |

전체 탭 기준 숫자가 모든 탭에 그대로 나온다면 필터 연동이 빠진 것이다.

추가로 확인한다.

1. 탭을 바꾸면 목록도 그 카테고리만 남는다
2. 업무 탭에서 항목 하나를 체크하면 `2 / 3 (67%)`로 바뀐다
3. 새로고침하면 전체 탭으로 돌아온다

**커밋**

```bash
git add index.html && git commit -m "feat: 카테고리 필터와 필터 기준 진행률"
```

---

## 5단계: 수정 기능과 마무리

**목표** — 마지막 기능인 인라인 수정을 붙이고, PRD의 완료 기준 7개를 전부 통과시킨다.

````
index.html에 할 일 수정 기능을 구현하고 마무리해 줘. PRD.md의 F-3 항목이다.

각 <li>에 수정 버튼을 추가하고, 이미 있는 이벤트 위임 리스너에서 처리해 줘
(data-action="edit"). 항목마다 리스너를 붙이지 마.

편집 모드는 editingId 변수 하나로 관리한다:
- 수정 버튼을 누르면 editingId에 그 항목의 id를 넣고 render()
- editingId는 한 번에 하나만 담기므로, 다른 항목의 수정을 누르면
  앞의 편집은 저장되지 않고 자동으로 닫힌다. 이 동작이 맞다

renderList()에서 항목의 id가 editingId와 같으면 그 <li>를 편집 모드로 그려 줘:
- 텍스트 자리에 현재 텍스트가 들어 있는 <input>
- 배지 자리에 현재 카테고리가 선택되어 있는 <select> (업무/개인/공부)
- 수정과 삭제 버튼 자리에 저장 버튼과 취소 버튼
- 이 <li>를 그린 직후 input에 포커스를 준다

saveEdit(id) 함수:
- input 값의 앞뒤 공백을 잘라 낸다
- 빈 문자열이면 저장하지 않고 편집 모드를 유지한다. 경고창을 띄우지 마
- 항목의 text와 category를 바꾸고, editingId를 null로 만든 뒤 saveTodos()와 render()

취소:
- editingId를 null로 만들고 render(). 값은 반영하지 않는다

키보드:
- 편집 input에서 Enter는 저장
- 편집 input에서 Esc는 취소

마무리로 CSS를 다듬어 줘:
- 버튼과 배지 크기를 맞춰 목록의 줄이 흔들리지 않게 한다
- 편집 모드로 바뀔 때 <li> 높이가 크게 출렁이지 않게 한다
- 마우스를 올렸을 때 항목이 반응하는 정도의 hover 효과만 넣는다.
  애니메이션을 넣지 마

작업이 끝나면 PRD.md의 10장 완료 기준 7개 항목을 하나씩 직접 점검하고,
통과 여부를 표로 정리해서 보고해 줘. 통과하지 못한 항목이 있으면 고친 뒤 다시 확인해.
````

**확인** (클로드가 수행하고 결과를 보고한다) — PRD 10장의 7개 항목을 전부 통과해야 한다. 그중 이 단계에서 새로 생긴 것들이다.

1. 항목의 텍스트와 카테고리를 함께 바꾸고 저장한다. 목록과 배지에 모두 반영된다
2. 새로고침해도 수정한 내용이 유지된다
3. 편집 중 Esc를 누르면 원래 값으로 돌아온다
4. 편집 중 텍스트를 모두 지우고 저장을 누른다. 저장되지 않고 편집 모드가 유지된다
5. 항목 A를 편집하다가 항목 B의 수정을 누른다. A는 원래 값으로 닫히고 B가 열린다

**커밋**

```bash
git add index.html && git commit -m "feat: 할 일 수정 기능과 스타일 마무리"
```

---

## 단계 요약

| 단계 | 내용 | PRD 항목 | 끝나면 할 수 있는 것 |
|---|---|---|---|
| 1 | 뼈대와 저장 계층 | F-7 | 콘솔로 넣은 데이터가 새로고침 후에도 남는다 |
| 2 | 할 일 추가 | F-1 | 앱에서 직접 할 일을 쌓을 수 있다 |
| 3 | 완료 체크와 삭제 | F-2, F-4 | 할 일을 끝내고 지울 수 있다 |
| 4 | 필터와 진행률 | F-5, F-6 | 카테고리별로 보고 진척을 확인한다 |
| 5 | 수정과 마무리 | F-3 | PRD의 모든 요구사항이 채워진다 |

## 마무리 점검 (사람이 브라우저에서 확인)

구현이 끝나면 배포 전에 직접 확인한다. 노트 4.1.5.3 b) 로컬 테스트가 이 자리다.

[PRD.md 10장 완료 기준](PRD.md#10-완료-기준)의 7개 항목을 순서대로 통과시킨다. 저장 유지, 진행률, 필터와 진행률 연동, 수정, 삭제, 빈 입력 차단, 빈 상태다.

하나라도 걸리면 그 자리에서 고치라고 지시한 뒤 다시 확인한다. 클로드의 완료 보고와 실제 화면이 다를 수 있고, 그 차이를 찾는 것이 이 단계의 목적이다.

여기에 하나를 더한다.

- 개발자 도구 Application → Local Storage에서 `todoApp.v1` 키를 연다. 값이 JSON 문자열 하나로 들어 있다 (4.4에서 다룰 내용을 눈으로 확인하는 자리)
