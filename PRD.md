<!-- 2026-08-09 16:36 KST -->

# 할 일 관리 앱 PRD

## 1. 개요

브라우저에서 바로 실행되는 개인용 할 일 관리 앱이다. 하루 10~20개 수준의 할 일을 적어 두고, 카테고리로 나눠 보면서 완료 여부와 진행률을 확인하는 것이 전부다.

서버도 계정도 없다. HTML 파일 하나를 브라우저로 열면 실행되고, 데이터는 브라우저 저장소에 남는다.

- 대상 사용자: 자기 할 일만 관리하는 한 명
- 실행 환경: 최신 데스크톱 브라우저 (크롬, 엣지, 사파리, 파이어폭스)
- 배포 방식: `index.html` 파일 하나를 더블클릭

## 2. 목표와 비목표

### 목표

- 할 일을 빠르게 적고, 끝내면 체크하는 흐름이 막힘없이 돌아갈 것
- 새로고침하거나 브라우저를 껐다 켜도 데이터가 그대로 남을 것
- 지금 얼마나 했는지 한눈에 보일 것
- 빌드 도구, 패키지 설치, 로컬 서버 없이 실행될 것

### 비목표

다음 기능은 이번 버전에 넣지 않는다. 요청 범위 밖이고, 10~20개 규모에서는 없어도 불편하지 않다.

- 마감일, 우선순위, 정렬 기능
- 검색, 반복 할 일, 하위 항목
- 로그인, 서버 동기화, 여러 기기 간 공유
- 알림, 통계, 되돌리기
- 카테고리 추가, 이름 변경

## 3. 사용자 시나리오

1. 아침에 앱을 연다. 어제 남긴 할 일이 그대로 보인다
2. 오늘 처리할 일을 입력창에 적고 카테고리를 고른 뒤 Enter를 누른다. 이 동작을 몇 번 반복한다
3. 일을 하나 끝낼 때마다 체크박스를 누른다. 상단 진행률 막대가 올라간다
4. 업무만 확인하고 싶으면 업무 탭을 누른다. 업무 항목만 남고 진행률도 업무 기준으로 바뀐다
5. 잘못 적은 항목은 수정 버튼으로 고치고, 필요 없어진 항목은 삭제한다
6. 브라우저를 닫는다. 다음에 다시 열면 그대로 남아 있다

## 4. 기능 요구사항

### F-1. 할 일 추가

- 입력창에 텍스트를 적고 카테고리를 고른 뒤 추가 버튼 또는 Enter로 등록
- 카테고리 기본값은 업무
- 새 항목은 목록 맨 위에 추가 (최근에 적은 것이 먼저 보이도록)
- 추가 후 입력창은 비우고, 커서는 입력창에 유지
- 텍스트가 비었거나 공백뿐이면 추가하지 않음
- 텍스트 앞뒤 공백은 잘라 내고 저장

### F-2. 완료 체크

- 각 항목의 체크박스로 완료와 미완료를 전환
- 완료된 항목은 텍스트에 취소선을 긋고 흐리게 표시
- 완료해도 목록에서 사라지지 않음 (직접 삭제하기 전까지 남음)
- 체크 즉시 진행률에 반영

### F-3. 할 일 수정

- 수정 버튼을 누르면 해당 줄이 편집 모드로 바뀜
- 편집 모드에서는 텍스트 입력창과 카테고리 선택이 그 자리에 나타남
- 저장 버튼 또는 Enter로 저장, 취소 버튼 또는 Esc로 되돌림
- 텍스트를 모두 지우고 저장하려 하면 저장하지 않음
- 한 번에 한 항목만 편집 모드로 둠

### F-4. 할 일 삭제

- 삭제 버튼을 누르면 확인 창을 띄움
- 확인하면 목록에서 제거하고 저장소에 반영
- 되돌리기는 제공하지 않음 (확인 창이 그 역할을 대신함)

### F-5. 카테고리 분류

- 카테고리는 업무, 개인, 공부 세 가지로 고정
- 모든 항목은 카테고리를 하나 가짐
- 목록에서 카테고리를 색상 배지로 표시
- 필터 탭: 전체 / 업무 / 개인 / 공부
- 선택한 탭에 해당하는 항목만 목록에 표시
- 필터 상태는 저장하지 않음 (앱을 다시 열면 항상 전체)

### F-6. 진행률 보기

- 상단에 진행률 막대와 `완료 수 / 전체 수 (퍼센트)` 표기
- **현재 선택된 필터에 걸린 항목을 기준으로 계산**한다. 업무 탭을 보고 있으면 업무 항목만으로 계산해, 화면에 보이는 목록과 숫자가 어긋나지 않도록 한다
- 퍼센트는 소수점 없이 반올림
- 해당 필터에 항목이 하나도 없으면 `0 / 0 (0%)`으로 표시하고 막대는 빈 상태

### F-7. 데이터 저장

- 목록이 바뀔 때마다 localStorage에 즉시 저장
- 앱을 열 때 localStorage에서 읽어 복원
- 저장 키는 `todoApp.v1`

## 5. 데이터 모델

localStorage에 JSON 문자열 하나로 저장한다.

```js
// localStorage["todoApp.v1"]
{
  version: 1,
  todos: [
    {
      id: "m2k7x1a9f3",          // 문자열, 항목마다 고유
      text: "PRD 검토 마무리",     // 앞뒤 공백 제거된 문자열
      category: "work",           // "work" | "personal" | "study"
      done: false,                // 불리언
      createdAt: 1754723760000    // 밀리초 타임스탬프
    }
  ]
}
```

- `category` 값과 화면 표기의 대응: `work` = 업무, `personal` = 개인, `study` = 공부
- `id`는 `Date.now().toString(36) + Math.random().toString(36).slice(2, 8)`로 만든다. `crypto.randomUUID()`는 브라우저에 따라 `file://`에서 동작이 갈릴 수 있어 쓰지 않는다
- `version`은 나중에 저장 형식이 바뀔 때를 대비한 표식이다. 이번 버전에서는 읽기만 하고 분기하지 않는다
- 화면에 보이는 순서는 배열 순서를 그대로 따른다. 새 항목은 배열 앞에 넣는다

## 6. 화면 설계

세로 한 화면으로 구성한다. 위에서 아래로 다음 순서다.

```
┌─────────────────────────────────────────┐
│  할 일 관리                              │
│  ▓▓▓▓▓▓▓▓▓▓░░░░░░░░  7 / 12 (58%)       │  ← 헤더 + 진행률
├─────────────────────────────────────────┤
│  [ 할 일을 입력하세요        ] [업무▾] [추가] │  ← 입력줄
├─────────────────────────────────────────┤
│  [ 전체 ] [ 업무 ] [ 개인 ] [ 공부 ]       │  ← 필터 탭
├─────────────────────────────────────────┤
│  ☐  PRD 검토 마무리        [업무] [수정][삭제]│
│  ☑  ~~장보기~~             [개인] [수정][삭제]│  ← 목록
│  ☐  자바스크립트 3장 읽기    [공부] [수정][삭제]│
└─────────────────────────────────────────┘
```

- 화면 폭은 최대 600px 정도로 두고 가운데 정렬
- 카테고리 배지는 세 가지 색으로 구분
- 항목이 하나도 없으면 목록 자리에 안내 문구를 표시. 전체 탭이면 "할 일을 추가해 보세요", 특정 카테고리 탭이면 "이 카테고리에는 할 일이 없습니다"
- 편집 모드일 때는 그 줄의 텍스트와 배지 자리에 입력창과 카테고리 선택이 들어가고, 수정과 삭제 버튼이 저장과 취소 버튼으로 바뀜

## 7. 동작 원리

- 메모리상의 배열 `todos`가 유일한 원본이다
- 상태가 바뀌는 모든 동작은 같은 절차를 따른다: 배열 변경 → localStorage 저장 → 목록 전체 다시 그리기
- 부분 갱신을 하지 않으므로 화면과 데이터가 어긋날 여지가 없다. 항목이 20개 안팎이라 전체를 다시 그려도 체감 지연이 없다
- 목록 안의 클릭은 목록 컨테이너에서 이벤트 위임으로 한 번에 처리한다. 항목마다 이벤트를 붙이지 않는다

## 8. 기술 스택과 제약

- HTML, CSS, 순수 자바스크립트만 사용. 라이브러리와 프레임워크 없음
- 파일 구성: **`index.html` 하나**. CSS는 `<style>`, 자바스크립트는 `<script>`로 같은 파일 안에 둔다
- ES 모듈(`import` / `export`)은 쓰지 않는다. `file://`로 열면 CORS 정책에 막혀 실행되지 않는다
- 빌드, 설치, 로컬 서버가 필요 없어야 한다

### localStorage 관련 주의

브라우저는 모든 `file://` 페이지를 같은 저장소로 취급한다. 다른 실습 파일도 같은 방식으로 열면 데이터가 섞일 수 있어, 저장 키를 `todoApp.v1`로 고유하게 잡았다.

## 9. 예외 처리

넣을 것만 넣는다.

| 상황 | 처리 |
|---|---|
| 빈 텍스트 또는 공백만 입력하고 추가 | 추가하지 않음. 입력창에 포커스 유지 |
| 편집 중 텍스트를 모두 지우고 저장 | 저장하지 않고 편집 모드 유지 |
| 삭제 버튼 클릭 | 확인 창을 띄우고, 확인한 경우에만 삭제 |
| localStorage에 저장된 값이 깨져 있음 | 파싱 실패를 잡아서 빈 목록으로 시작 |

저장 용량 초과는 처리하지 않는다. 항목 20개 규모에서는 발생하지 않는다.

## 10. 완료 기준

다음 항목이 모두 통과하면 완성으로 본다. 브라우저에서 직접 확인한다.

1. **저장 유지** — 할 일 3개를 추가하고 새로고침한다. 3개가 그대로 있다. 브라우저를 완전히 닫았다 다시 열어도 남아 있다
2. **진행률** — 4개 중 1개를 체크하면 `1 / 4 (25%)`가 되고 막대가 4분의 1만큼 찬다. 체크를 해제하면 `0 / 4 (0%)`로 돌아온다
3. **필터와 진행률 연동** — 업무 3개(1개 완료), 개인 2개(2개 완료)를 넣는다. 전체 탭은 `3 / 5 (60%)`, 업무 탭은 `1 / 3 (33%)`, 개인 탭은 `2 / 2 (100%)`를 보여 준다
4. **수정** — 항목의 텍스트와 카테고리를 함께 바꾼다. 저장하면 목록과 배지에 반영되고, 새로고침해도 유지된다. Esc를 누르면 원래대로 돌아온다
5. **삭제** — 확인 창이 뜨고, 취소하면 남고 확인하면 사라진다. 새로고침해도 사라진 상태다
6. **빈 입력 차단** — 아무것도 입력하지 않고 추가를 누른다. 아무 일도 일어나지 않는다. 공백만 입력해도 마찬가지다
7. **빈 상태** — 항목을 모두 지우면 안내 문구가 나온다. 항목이 있는 상태에서 비어 있는 카테고리 탭을 눌러도 안내 문구가 나온다

## 11. 나중에 생각해 볼 것

이번 범위에는 넣지 않지만, 쓰다 보면 필요해질 만한 것들을 적어 둔다.

- 완료 항목 일괄 삭제
- 마감일과 지난 항목 표시
- 드래그로 순서 바꾸기
- JSON 내려받기와 불러오기로 백업
- 다크 모드
